apply plugin: 'java'
apply plugin: 'maven'
apply plugin: 'application'

group="localdomain.localhost"
version="1.0-SNAPSHOT"

mainClassName = "localdomain.localhost.nested.Start"

install.dependsOn installApp
assemble.dependsOn distZip

repositories {
    mavenCentral()
    mavenLocal()
}

task wrapper(type: Wrapper) {
    gradleVersion = '1.10'
}